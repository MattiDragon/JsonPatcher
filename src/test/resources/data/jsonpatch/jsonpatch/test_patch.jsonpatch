@version "1";
@target {
    "regex": "minecraft:recipes/.*arrow\\.json"
};

function modifyKey($, result) {
    $'#' = {
        tag: "minecraft:coals"
    };

    if (result.id == "minecraft:arrow") delete $X;
}

# Skip the tipped arrow recipe
if ($type != "minecraft:crafting_shaped") return;


val result = $result;
modifyKey($key, result);

if (result.id == "minecraft:arrow") {
    $pattern[0] = "#";
}
result.count += math.abs(-32);

